## 🌐 Frontend - Sports Center (Day 23)

This branch `frontendstart` is the beginning of the frontend portion of the Sports Center full-stack project built using **React + Vite**.

### ✅ Day 23 Progress
- Initialized frontend branch with Vite and React
- Set up project structure for scalability
- Prepared to build reusable components

### 🔜 Upcoming Goals
- [ ] Create a responsive `AppBar` component
- [ ] Develop a `ThemeBuilder` for consistent light/dark themes
- [ ] Integrate `ThemeProvider` using MUI or styled-components
- [ ] Make the `AppBar` static across all routes/pages

Stay tuned for more UI progress 🚀
